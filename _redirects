# In-app views: serve the SPA shell; app.js opens the matching view.
# Status 200 = rewrite (URL stays /news), not a redirect.
/news    /index.html   200
/news/   /index.html   200
/info    /index.html   200
/info/   /index.html   200
# Everything else that doesn't exist falls through to /404.html (Netlify default).
